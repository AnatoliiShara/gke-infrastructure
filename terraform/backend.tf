# Backend configuration for remote state storage
# Uncomment and configure when billing is enabled

# terraform {
#   backend "gcs" {
#     bucket = "gke-infrastructure-terraform-state-bucket"
#     prefix = "terraform/state"
#   }
# }
